name: Deploy to Render

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Render deploy
        run: |
          curl -s -o /dev/null -w "HTTP %{http_code}\n" \
            --request POST "${{ secrets.RENDER_DEPLOY_HOOK }}"
